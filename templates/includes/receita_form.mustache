<div class="form-group">
  <label for="inputCuidado">Cuidado</label>
  <input
    type="text"
    class="form-control"
    id="inputCuidado"
    placeholder="Cuidado..."
    value="{{ cuidado_value }}"
    name="cuidado"
  />
</div>
<div class="form-group">
  <label for="inputReceita">Receita</label>
  <input
    type="text"
    class="form-control"
    id="inputReceita"
    placeholder="Receita..."
    value="{{ receita_value }}"
    name="receita"
  />
</div>
<a href="{{ document_uri }}" class="{{ link_class }}" target="_blank" rel="noreferrer noopener">URL do ficheiro da receita</a><br/>
<input type="hidden" value="{{ id_receita }}" name="idreceita"/>
