<div class="container">
    <table class="table table-striped table-dark table-hover">
        <thead>
        <tr>
            <th scope="col">Nome</th>
            <th scope="col">Nº Utente</th>
            <th scope="col">Cidade</th>
            <th scope="col">Consultas por fazer</th>
            <th scope="col">Detalhes</th>
        </tr>
        </thead>
        <tbody>
        {{#utentes}}
            <tr>
                <td>{{ nome }}</td>
                <td>{{ numero_utente }}</td>
                <td>{{ cidade }}</td>
                <td><a href="{{ consultas_uri }}">{{ consultas_inacabadas }}</a></td>
                <td>{{ detalhes_uri }}</td>
            </tr>
        {{/utentes}}
        </tbody>
    </table>
    <div class="paginacao">
        {{&pagination}}
    </div>
</div>
